CREATE TABLE IF NOT EXISTS folderMemberships (folder_id INTEGER NOT NULL, transaction_id INTEGER NOT NULL,FOREIGN KEY(folder_id) REFERENCES folders(id), FOREIGN KEY(transaction_id) REFERENCES transactions(id));