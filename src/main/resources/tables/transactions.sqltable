create table if not exists transactions (id integer not null primary key autoincrement, account integer not null, category integer not null, name text, location text, date_creation text, date_application text, amount float, output boolean default false, state text not null default "done", foreign key(account) references accounts(id), foreign key(category) references categories(id));