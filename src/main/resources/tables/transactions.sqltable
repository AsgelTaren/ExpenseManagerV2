create table if not exists transactions (id integer not null primary key auto_increment, account integer not null, category integer not null, name text, location text, date_creation text, date_application text, amount float, output boolean default false, done boolean default false, foreign key(account) references accounts(id), foreign key(category) references categories(id));